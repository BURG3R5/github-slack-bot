version: '3'

services:
  proton:
    build:
      context: .
    container_name: proton
    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"
    volumes:
      - type: volume
        source: proton
        target: /github-slack-bot/data

volumes:
  proton:
    name: "proton"
